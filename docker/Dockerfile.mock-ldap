# Mock LDAP server for development/testing
# Build context: repository root (.)

FROM python:3.11-slim
WORKDIR /app

COPY example_apps/mock_ldap_server/ldap_server.py ./

EXPOSE 1389
CMD ["python", "ldap_server.py", "--host", "0.0.0.0", "--port", "1389"]
