%% System Context - Frontier AI Chat Platform
flowchart TB
    subgraph People
        user(("End User"))
        admin(("Administrator"))
    end

    frontier["Frontier Platform<br/>Multi-project AI chat platform with RBAC"]

    subgraph External["External Systems"]
        ldap["LDAP Server<br/>Corporate directory for authentication"]
        langgraph["LangGraph Agent<br/>LangGraph-based AI agent"]
        openai["OpenAI API<br/>OpenAI-compatible AI service"]
        http_agent["HTTP Agent<br/>Custom HTTP streaming agent"]
    end

    user -->|Uses / HTTPS| frontier
    admin -->|Manages / HTTPS| frontier
    frontier -->|Authenticates / LDAP| ldap
    frontier -->|Streams messages / HTTP-SSE| langgraph
    frontier -->|Streams messages / HTTP-SSE| openai
    frontier -->|Streams messages / HTTP-SSE| http_agent
